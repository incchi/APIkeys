// urls - 
    // http://localhost:4000api/register
    // http://localhost:400/api/login
    // http://localhost:400/api/generatekey
    // http://localhost:400/api/apilimit    // premium user can get 5 api hits in a min else default get 2
    // http://localhost:400/api/addstore   // only admin can add store
    // http://localhost:400/api/updatestore // either admin or manager can edit the store list 
    // http://localhost:400/api/nearbystores  // user can check the nearby stores to him
    


// keys keys are of two types 
    // 1 premium these can hit api 5 times a min
    // 2 default these can hit api only 2 times a min

// key Roles
    // 1 admin 
    // 2 manager
    // 3 user

